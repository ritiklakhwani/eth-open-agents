// sprite-gen — Replicate API client to convert uploaded photos to pixel-art sprites
// Owner: Ritik. Phase 7b (~3h).
//
// Used by /api/pets/sprite endpoint when judges/users upload a photo during adoption.
// Output sprite is then bundled into the pet's identity blob and stored on 0G.
//
// Will export:
//   - pixelatePhoto(file: File): Promise<{ spriteUrl: string }>
//
// Pick a Replicate pixel-art model — candidates:
//   - fofr/sticker-maker
//   - pwntus/pixel-art-style
//   - cjwbw/anything-v3-better-vae

export {}
